apiVersion: apps/v1
kind: Deployment
metadata:
  name: vllm
  labels:
    app: vllm
    gitops-test: automated-sync
  annotations:
    gitops.example.com/managed-by: argocd

spec:
  replicas: 1

  selector:
    matchLabels:
      app: vllm

  template:
    metadata:
      labels:
        app: vllm

    spec:
      containers:
        - name: vllm
          image: ghcr.io/sachin001234/vllm-platform:b2fdadc53eab4c42e7ca657400a114f8a2311722

          ports:
            - containerPort: 8000
              protocol: TCP

          readinessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 10
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 6

          livenessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 30
            periodSeconds: 20
            timeoutSeconds: 5
            failureThreshold: 3

          args:
            - --model
            - Qwen/Qwen2.5-1.5B-Instruct-AWQ
            - --quantization
            - awq
            - --gpu-memory-utilization
            - "0.75"
            - --max-model-len
            - "512"
            - --enforce-eager
